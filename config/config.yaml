MODEL:
  CONTEXT_LEN: 32  # how many chars the model looks back
  EMBED_DIM: 32    # embedding size
  N_HEADS: 2       # attention heads
  N_LAYERS: 2      # transformer blocks

TRAINING:
  BATCH_SIZE: 64
  LR: 3e-4
  STEPS: 1000